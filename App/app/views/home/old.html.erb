<!DOCTYPE html>
<html lang="en">
<head>
<title>CSS Template</title>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<%= stylesheet_link_tag "home" %>

</head>
<body>
<div class = "dropdown" style="float:right;">
  <button class="dropbtn">User Settings</button>
  <div class="dropdown-content">
    <%= link_to("Settings", users_path(session[:user_id]))%>
    <%= link_to("Inbox", users_path(session[:user_id]))%>
    <%= button_to("Logout", '/logout',  {method: :post, class: 'button'})%>
  </div>
</div>
<div class="header">
  <h2>Home</h2>
</div>

<section id = "navbar", class = "wrapper">
    <ul>
      <li> <a href="/home">Home</a> </li>
      <li><a href="/posts">Posts</a> </li>
      <li><a href="/tags">Tags</a> </li>
    <ul>
</section>


<div class="content">
  <h2>Top Posts</h2>
    <div class = "container">
      <% @posts.each do |post|%>
        <div>
          <%= link_to(post.title, post_path(post))%> 
          <%= post.body%>
            <ul>
              <% post.tags.each do |tag|%>
                <li><%= link_to(tag.name, tag_path(tag))%> </li>
              <%end%>
            </ul>
        </div>
      <%end%>
    </div>
</div>

</body>
</html>