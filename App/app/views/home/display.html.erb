<!DOCTYPE html>
<html lang="en">
<head>
<title>CSS Template</title>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
* {
  box-sizing: border-box;
  font-family: Arial, Helvetica, sans-serif;
}

.header {
  background-color: #f1f1f1;
  padding: 10px;
  text-align: center;
  font-size: 35px;
}

body {
  margin: 0;
  font-family: Arial, Helvetica, sans-serif;
}

/* Style the top navigation bar */
.topnav {
  overflow: hidden;
  background-color: #333;
}

/* Style the topnav links */
.topnav a {
  float: left;
  display: block;
  color: #f2f2f2;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
}

/* Change color on hover */
.topnav a:hover {
  background-color: #ddd;
  color: black;
}

/* Style the content */
.content {
  text-align: center;
  background-color: #ddd;
  padding: 10px;
  height: 510px;
  overflow:auto;
}


.dropbtn {
  background-color: grey;
  color: white;
  padding: 16px;
  font-size: 16px;
  border: none;
  cursor: pointer;
}

/* The container <div> - needed to position the dropdown content */
.dropdown {
  position: relative;
  display: inline-block;
}

/* Dropdown Content (Hidden by Default) */
.dropdown-content {
  display: none;
  position: absolute;
  background-color: #f9f9f9;
  box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
  z-index: 1;
  width: 128px;
}

/* Links inside the dropdown */
.dropdown-content form {
  background-color: white;
  color: black;
  padding: 12px 16px;
  text-decoration: none;
  display: block;
  width: 128px;
}

.button {
  background-color: white; /* Green */
  border: none;
  color: black;
  text-align: center;
  text-decoration: none;
  display: inline-block;
  font-size: 15px;
  width: 100px;
}

.dropdown:hover button{background-color: #f1f1f1}

/* Show the dropdown menu on hover */
.dropdown:hover .dropdown-content {
  display: block;
  background-color: #f1f1f1;
}
.dropdown:hover .dropbtn {background-color: grey;}
.dropdown:hover form:hover {background-color:  #f1f1f1;}
</style>

</head>
<body>
<div class = "dropdown" style="float:right;">
  <button class="dropbtn">User Settings</button>
  <div class="dropdown-content">
    <%= button_to("User Settings", users_path(session[:user_id]), {form_class: 'dropdown-content form' class: 'button'})%>
    <%= button_to("Inbox", users_path(session[:user_id]),  {class: 'button'})%>
    <%= button_to("Logout", '/logout',  {method: :post, class: 'button'})%>
  </div>
</div>
<div class="header">
  <h2>Home</h2>
</div>

<div class="topnav">
  <a href="/home">Home</a>
  <a href="/posts">Posts</a>
  <a href="/tags">Tags</a>
</div>


<div class="content">
  <h2>Top Posts</h2>
  <%@posts.each do |post|%>
    <%= content_tag(:h2, link_to(post.title, post_path(post))) %>
    <%post.tags.each do |tag|%>
        <%=link_to tag.name, tag_path(tag)%> <br>
    <%end%>
    <%end%>
</div>


</body>
</html>